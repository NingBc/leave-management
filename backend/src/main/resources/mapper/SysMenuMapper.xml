<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.leave.system.mapper.SysMenuMapper">

    <select id="selectAllMenus" resultType="SysMenu">
        SELECT * FROM sys_menu ORDER BY parent_id, order_num, id
    </select>

    <select id="selectMenuById" resultType="SysMenu">
        SELECT * FROM sys_menu WHERE id = #{id}
    </select>

    <select id="selectMenusByIds" resultType="SysMenu">
        SELECT * FROM sys_menu WHERE id IN
        <foreach collection="list" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
        ORDER BY parent_id, order_num, id
    </select>

    <insert id="insertMenu" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO sys_menu (parent_id, menu_name, path, component, icon, order_num)
        VALUES (#{parentId}, #{menuName}, #{path}, #{component}, #{icon}, #{orderNum})
    </insert>

    <update id="updateMenu">
        UPDATE sys_menu
        <set>
            <if test="parentId != null">parent_id = #{parentId},</if>
            <if test="menuName != null">menu_name = #{menuName},</if>
            <if test="path != null">path = #{path},</if>
            <if test="component != null">component = #{component},</if>
            <if test="icon != null">icon = #{icon},</if>
            <if test="orderNum != null">order_num = #{orderNum},</if>
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteMenuById">
        DELETE FROM sys_menu WHERE id = #{id}
    </delete>

</mapper>
